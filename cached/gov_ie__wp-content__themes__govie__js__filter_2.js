try {    jQuery(document).ready(function() {	// if we can run this, hide the letter index	jQuery('#letters').hide();	// set the placeholder if the browser doesn't support it	if (!('placeholder' in document.createElement('input'))) {	    jQuery('input[placeholder]').each(function () { jQuery(this).watermark(jQuery(this).attr("placeholder")) });	}		// we want a case-insensitive search	jQuery.expr[':'].Contains = function(a,i,m){	    return (a.textContent || a.innerText || "").toUpperCase().indexOf(m[3].toUpperCase())>=0;	};	jQuery('#filter').change(function() {	    var filter = jQuery(this).val();	    jQuery(".filterable li a:Contains(" + filter + ")").parent().show(); // show	    jQuery(".filterable li a:not(:Contains(" + filter + "))").parent().hide(); // hide            return false;	    	});	jQuery('#filter').keyup(function() {	    jQuery(this).change()	});    });} catch (e) {    // probably no jQuery loaded    // do nothing}